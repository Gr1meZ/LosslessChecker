<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="BgPrimaryBrush" Color="#1e1e1e"/>
    <SolidColorBrush x:Key="BgSecondaryBrush" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="BgTertiaryBrush" Color="#252525"/>
    <SolidColorBrush x:Key="FgPrimaryBrush" Color="#cccccc"/>
    <SolidColorBrush x:Key="FgSecondaryBrush" Color="#999999"/>
    <SolidColorBrush x:Key="FgMutedBrush" Color="#808080"/>
    <SolidColorBrush x:Key="FgSubtleBrush" Color="#5a5a5a"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#3e3e3e"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#4a9eff"/>
    <SolidColorBrush x:Key="GridAltRowBrush" Color="#252525"/>
    <SolidColorBrush x:Key="LosslessGreenBrush" Color="#4ec9b0"/>
    <SolidColorBrush x:Key="SuspiciousAmberBrush" Color="#cca700"/>
    <SolidColorBrush x:Key="FakeRedBrush" Color="#f14c4c"/>
    <SolidColorBrush x:Key="NeutralGrayBrush" Color="#808080"/>

    <SolidColorBrush x:Key="GlassBrush" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#3e3e3e"/>
    <SolidColorBrush x:Key="CardBrush" Color="#252525"/>

    <SolidColorBrush x:Key="AccentGradientBrush" Color="#4a9eff"/>
    <SolidColorBrush x:Key="ProgressGradientBrush" Color="#4a9eff"/>

    <SolidColorBrush x:Key="VerdictGoodBrush" Color="#4ec9b0"/>
    <SolidColorBrush x:Key="VerdictWarnBrush" Color="#cca700"/>
    <SolidColorBrush x:Key="VerdictBadBrush" Color="#f14c4c"/>

    <DropShadowEffect x:Key="ShadowE1" BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE2" BlurRadius="16" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE3" BlurRadius="24" ShadowDepth="8" Opacity="0.5" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowGlow" BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="#4a9eff"/>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE2}"/>
    </Style>

    <Style x:Key="GlassCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#3d3d3d"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="#505050"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#505050"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Background" Value="#3d2020"/>
        <Setter Property="Foreground" Value="{StaticResource FakeRedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FakeRedBrush}"/>
    </Style>

</ResourceDictionary>
