<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================ -->
    <!--  STUDIO MONITOR DARK THEME                                   -->
    <!--  Deep navy/charcoal palette inspired by pro audio consoles   -->
    <!-- ============================================================ -->

    <!-- ===== BACKGROUNDS ===== -->
    <SolidColorBrush x:Key="BgPrimaryBrush" Color="#0f1117"/>
    <SolidColorBrush x:Key="BgSecondaryBrush" Color="#1a1d27"/>
    <SolidColorBrush x:Key="BgTertiaryBrush" Color="#222639"/>
    <SolidColorBrush x:Key="CardBrush" Color="#161922"/>
    <SolidColorBrush x:Key="GlassBrush" Color="#1a1d27"/>
    <SolidColorBrush x:Key="GridAltRowBrush" Color="#141720"/>

    <!-- ===== FOREGROUNDS ===== -->
    <SolidColorBrush x:Key="FgPrimaryBrush" Color="#e8eaed"/>
    <SolidColorBrush x:Key="FgSecondaryBrush" Color="#9aa0b0"/>
    <SolidColorBrush x:Key="FgMutedBrush" Color="#6b7280"/>
    <SolidColorBrush x:Key="FgSubtleBrush" Color="#4b5162"/>

    <!-- ===== BORDERS ===== -->
    <SolidColorBrush x:Key="BorderBrush" Color="#2a2e3f"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#2a2e3f"/>

    <!-- ===== ACCENT ===== -->
    <SolidColorBrush x:Key="AccentBrush" Color="#60a5fa"/>
    <SolidColorBrush x:Key="AccentGradientBrush" Color="#60a5fa"/>

    <!-- ===== STATUS SIGNAL COLORS ===== -->
    <SolidColorBrush x:Key="SignalGreenBrush" Color="#34d399"/>
    <SolidColorBrush x:Key="SignalAmberBrush" Color="#fbbf24"/>
    <SolidColorBrush x:Key="SignalRedBrush" Color="#f87171"/>
    <SolidColorBrush x:Key="NeutralGrayBrush" Color="#6b7280"/>

    <!-- ===== STATUS BACKGROUNDS (for translucent badges) ===== -->
    <SolidColorBrush x:Key="SignalGreenBgBrush" Color="#2634d399"/>
    <SolidColorBrush x:Key="SignalAmberBgBrush" Color="#26fbbf24"/>
    <SolidColorBrush x:Key="SignalRedBgBrush" Color="#26f87171"/>

    <!-- ============================================================ -->
    <!--  SHADOW EFFECTS                                              -->
    <!-- ============================================================ -->
    <DropShadowEffect x:Key="ShadowE1" BlurRadius="8" ShadowDepth="2" Opacity="0.4" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE2" BlurRadius="16" ShadowDepth="4" Opacity="0.5" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE3" BlurRadius="24" ShadowDepth="8" Opacity="0.6" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowGlow" BlurRadius="20" ShadowDepth="0" Opacity="0.35" Color="#60a5fa"/>
    <DropShadowEffect x:Key="ShadowGreenGlow" BlurRadius="16" ShadowDepth="0" Opacity="0.3" Color="#34d399"/>
    <DropShadowEffect x:Key="ShadowAmberGlow" BlurRadius="16" ShadowDepth="0" Opacity="0.3" Color="#fbbf24"/>
    <DropShadowEffect x:Key="ShadowRedGlow" BlurRadius="16" ShadowDepth="0" Opacity="0.3" Color="#f87171"/>

    <!-- ============================================================ -->
    <!--  BASE STYLES                                                 -->
    <!-- ============================================================ -->

    <!-- CardStyle -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
    </Style>

    <!-- GlassCardStyle -->
    <Style x:Key="GlassCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassBrush}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
    </Style>

    <!-- ============================================================ -->
    <!--  BUTTON STYLES                                               -->
    <!-- ============================================================ -->

    <!-- PrimaryButtonStyle - filled, used for main actions -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#2a2e3f"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="#3b3f54"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}"
                            Effect="{TemplateBinding Effect}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#3b3f54"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- SecondaryButtonStyle - bordered, for secondary actions -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#2a2e3f"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DangerButtonStyle -->
    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
        <Setter Property="Background" Value="#2d1517"/>
        <Setter Property="Foreground" Value="{StaticResource SignalRedBrush}"/>
        <Setter Property="BorderBrush" Value="#4a1f22"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4a1f22"/>
                <Setter Property="BorderBrush" Value="{StaticResource SignalRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================================ -->
    <!--  TOOLTIP STYLE                                               -->
    <!-- ============================================================ -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource BgSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE2}"/>
    </Style>

    <!-- ============================================================ -->
    <!--  SCROLLBAR STYLES                                            -->
    <!-- ============================================================ -->


    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- ============================================================ -->
    <!--  CONTEXT MENU STYLE                                          -->
    <!-- ============================================================ -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource BgSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE2}"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
