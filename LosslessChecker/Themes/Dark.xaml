<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="BgPrimaryBrush" Color="#0f0f1a"/>
    <SolidColorBrush x:Key="BgSecondaryBrush" Color="#1a1a2e"/>
    <SolidColorBrush x:Key="BgTertiaryBrush" Color="#16162a"/>
    <SolidColorBrush x:Key="FgPrimaryBrush" Color="#e2e8f0"/>
    <SolidColorBrush x:Key="FgSecondaryBrush" Color="#cbd5e1"/>
    <SolidColorBrush x:Key="FgMutedBrush" Color="#94a3b8"/>
    <SolidColorBrush x:Key="FgSubtleBrush" Color="#475569"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#818cf8"/>
    <SolidColorBrush x:Key="GridAltRowBrush" Color="#1e1e34"/>
    <SolidColorBrush x:Key="LosslessGreenBrush" Color="#34d399"/>
    <SolidColorBrush x:Key="SuspiciousAmberBrush" Color="#fbbf24"/>
    <SolidColorBrush x:Key="FakeRedBrush" Color="#f87171"/>
    <SolidColorBrush x:Key="NeutralGrayBrush" Color="#64748b"/>

    <SolidColorBrush x:Key="GlassBrush" Color="#1a1a2e"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#2d2d4a"/>
    <SolidColorBrush x:Key="CardBrush" Color="#16162a"/>

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#818cf8" Offset="0"/>
        <GradientStop Color="#6366f1" Offset="0.5"/>
        <GradientStop Color="#8b5cf6" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ProgressGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#6366f1" Offset="0"/>
        <GradientStop Color="#818cf8" Offset="0.5"/>
        <GradientStop Color="#a78bfa" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VerdictGoodBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#059669" Offset="0"/>
        <GradientStop Color="#10b981" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VerdictWarnBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#d97706" Offset="0"/>
        <GradientStop Color="#f59e0b" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VerdictBadBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#dc2626" Offset="0"/>
        <GradientStop Color="#ef4444" Offset="1"/>
    </LinearGradientBrush>

    <DropShadowEffect x:Key="ShadowE1" BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE2" BlurRadius="16" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowE3" BlurRadius="24" ShadowDepth="8" Opacity="0.5" Color="#000000"/>
    <DropShadowEffect x:Key="ShadowGlow" BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="#818cf8"/>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE2}"/>
    </Style>

    <Style x:Key="GlassCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentGradientBrush}"/>
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect" Value="{StaticResource ShadowE1}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowGlow}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FgPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Background" Value="#2a1518"/>
        <Setter Property="Foreground" Value="{StaticResource FakeRedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FakeRedBrush}"/>
    </Style>
</ResourceDictionary>
