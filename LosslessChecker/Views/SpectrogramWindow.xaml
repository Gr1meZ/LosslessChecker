<Window x:Class="LosslessChecker.Views.SpectrogramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spectrogram" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#11111b" Foreground="{DynamicResource FgPrimaryBrush}"
        ResizeMode="CanResizeWithGrip"
        MouseWheel="Window_MouseWheel"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove"
        SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Background="{DynamicResource BgSecondaryBrush}">
            <Button Content="📋 Copy PNG" Click="CopyPng_Click" Background="{DynamicResource BorderBrush}"
                    Foreground="{DynamicResource FgPrimaryBrush}" BorderThickness="0" Margin="4" Cursor="Hand"/>
            <Button Content="↺ Reset" Click="ResetZoom_Click" Background="{DynamicResource BorderBrush}"
                    Foreground="{DynamicResource FgPrimaryBrush}" BorderThickness="0" Margin="4" Cursor="Hand"/>
        </ToolBar>

        <Image x:Name="SpectrogramImage" Grid.Row="1" Stretch="Uniform"
               Margin="4" RenderOptions.BitmapScalingMode="HighQuality"/>

        <StatusBar Grid.Row="2" Background="{DynamicResource BgPrimaryBrush}">
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource FgMutedBrush}" FontSize="11"/>
        </StatusBar>
    </Grid>
</Window>
