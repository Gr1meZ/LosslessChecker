# 9 багов — План реализации

> **Для agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Исправить 9 UX/UI багов: welcome screen, дозапись файлов, обложка, спектрограмма, оценка альбома, подсветка, вердикты.

**Tech Stack:** .NET 10 WPF, NAudio, NWaves, CommunityToolkit.Mvvm, TagLibSharp.

---

## File Map

| File | Action | Что меняется |
|------|--------|-------------|
| `Views/MainWindow.xaml` | MODIFY | Убрать WelcomeOverlay, поменять на Visibility-toggling; бейджи альбома в TreeView; cover binding; DataGrid selection brushes; verdict bar; кнопка Добавить |
| `Views/MainWindow.xaml.cs` | MODIFY | Drag-drop для списка файлов/папок; новый ScanAndAppend |
| `Views/MainWindow.xaml.cs` | MODIFY | Drag-drop handler для multiple items |
| `ViewModels/MainViewModel.cs` | MODIFY | ShowWelcome вместо IsEmpty; ScanAndAppend; AlbumGroup поля |
| `ViewModels/AudioFileViewModel.cs` | MODIFY | VerdictLabel property; почему-блок в BuildMetricItems |
| `Models/GroupModels.cs` | MODIFY | AlbumGroup: агрегированные поля |
| `Models/AnalysisResult.cs` | MODIFY | WhyVerdict поле |
| `Services/SpectrogramBuilder.cs` | MODIFY | FreqBins 256→512, MaxFrames 300→600 |
| `Services/SpectrogramRenderer.cs` | MODIFY | Убрать DrawText/DrawHLine/DrawVLine, вернуть только битмап |
| `Views/SpectrogramWindow.xaml` | MODIFY | Canvas с осями поверх Image |
| `Views/SpectrogramWindow.xaml.cs` | MODIFY | Отрисовка осей/сетки/cutoff через XAML-элементы |
| `Services/VerdictGenerator.cs` | MODIFY | Генерация WhyVerdict |

---

### Task 1: Welcome screen fix (баги 1-2)

**Files:** `MainWindow.xaml`, `MainWindow.xaml.cs`, `MainViewModel.cs`

**Шаги:**

1. В MainWindow.xaml:
   - Убрать `WelcomeOverlay` Grid (строки с `x:Name="WelcomeOverlay"`)
   - Обернуть основной контент (Grid с RowDefinitions) в `Border x:Name="MainContent"`
   - Приветственный экран вынести в отдельный `Border x:Name="WelcomeContent"` (не перекрывающий, а альтернативный)
   - Переключать через `Visibility="{Binding ShowWelcome, Converter={StaticResource BoolToVis}}"` на MainContent и инвертированный на WelcomeContent

2. В MainViewModel.cs:
   - Переименовать `_isEmpty` в `_showWelcome`, начальное значение `true`
   - В `ScanAndAnalyze`: `ShowWelcome = false` в первой строке (до Clear)
   - НЕ ставить `ShowWelcome = true` в finally — только если Files.Count == 0 после обработки
   - В `SelectFolder`: `ShowWelcome = false` перед вызовом `ScanAndAnalyze`

3. Build + test + commit

---

### Task 2: Множественный выбор + дозапись (баги 3-4)

**Files:** `MainWindow.xaml`, `MainWindow.xaml.cs`, `MainViewModel.cs`, `FileScanner.cs`

**Шаги:**

1. В MainWindow.xaml — добавить кнопку «📁 Добавить» в тулбар (рядом с «Выбрать папку»)

2. В MainViewModel.cs — новый метод `ScanAndAppend(IEnumerable<string> paths)`:
   - Собрать все файлы: для каждого path — если файл и подходящее расширение → добавить; если папка → `FileScanner.ScanFolder`
   - Отфильтровать дубликаты по `FilePath` (сравнивать с существующими `Files`)
   - Создать `AudioFileViewModel` для новых, добавить в `Files`
   - Запустить анализ только для новых (ConcurrentQueue только из новых)
   - `PopulateArtistGroups()` перестроить
   - `ShowWelcome = false`

3. В MainViewModel.cs — новый `[RelayCommand] AddFiles()`:
   - `OpenFileDialog` с `Multiselect=true`, `Filter = "Audio Files|*.mp3;*.flac;*.wav;*.m4a;*.alac"`
   - Вызвать `ScanAndAppend` с выбранными путями

4. В MainWindow.xaml.cs — переписать `Window_Drop`:
   - Взять ВСЕ `files` из `e.Data.GetData(DataFormats.FileDrop)`
   - Передать все пути в `ScanAndAppend`

5. Build + test + commit

---

### Task 3: Обложка в карточке трека (баг 5)

**Files:** `MainWindow.xaml`

**Шаги:**

1. В MainWindow.xaml строка 342: сменить `{Binding SelectedGroup.CoverData}` на `{Binding SelectedFile.CoverData}` у Image Source
2. Плэйсхолдер (🎵) уже привязан через `NullToVisibility` — проверить, что работает с `SelectedFile.CoverData`
3. Build + commit

---

### Task 4: Спектрограмма (баг 6)

**Files:** `SpectrogramBuilder.cs`, `SpectrogramRenderer.cs`, `SpectrogramWindow.xaml`, `SpectrogramWindow.xaml.cs`

**Шаги:**

1. В `SpectrogramBuilder.cs`:
   - `FreqBins = 512` (было 256)
   - `MaxFrames = 600` (было 300)

2. В `SpectrogramRenderer.cs`:
   - Убрать поля `LeftMargin`, `RightMargin`, `TopMargin`, `BottomMargin`
   - `Render()` — возвращать только битмап dataWidth×dataHeight (без margins, без осей, без cutoff)
   - Убрать методы `DrawText`, `DrawHLine`, `DrawVLine`
   - Оставить только отрисовку данных спектрограммы + HotColormap

3. В `SpectrogramWindow.xaml`:
   - Заменить простой Image на Grid с Canvas поверх:
   ```xml
   <Grid>
     <Image x:Name="SpectrogramImage" Stretch="Uniform"/>
     <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False"/>
   </Grid>
   ```

4. В `SpectrogramWindow.xaml.cs`:
   - Убрать `SpectrogramRenderer _renderer` поле
   - После загрузки битмапа — отрисовать на `OverlayCanvas`:
     - Частотные метки (TextBlock, позиция Y по log-шкале)
     - Временные метки (TextBlock, позиция X линейно)
     - Горизонтальные/вертикальные линии сетки
     - Красная пунктирная линия cutoff
   - В `RenderFull()` — очищать Canvas.Children и перестраивать

5. В `AudioFileViewModel.GetOrBuildSpectrogram` — убрать передачу `durationSec`, `sampleRate`, `cutoffHz` в `SpectrogramRenderer.Render` (теперь только `_rawSpectro`, `_spectroWidth`, `_spectroHeight`)

6. В `MainWindow.xaml.cs` `Spectrogram_Click` — передавать длительность/sampleRate/cutoffHz в конструктор `SpectrogramWindow` (для осей)

7. Build + test + commit

---

### Task 5: Оценка альбома (баг 7)

**Files:** `GroupModels.cs`, `MainViewModel.cs`, `MainWindow.xaml`

**Шаги:**

1. В `GroupModels.cs` — добавить в `AlbumGroup`:
   ```csharp
   public double AverageLosslessScore { get; set; }
   public double AverageQualityScore { get; set; }
   public double AverageDynamicRange { get; set; }
   public string AlbumVerdict { get; set; } = "";
   public int KeepCount { get; set; }
   public int InvestigateCount { get; set; }
   public int ReplaceCount { get; set; }
   public int TotalTracks => Tracks.Count;
   ```

2. В `MainViewModel.cs` `PopulateArtistGroups` — после создания каждого AlbumGroup вычислить агрегаты:
   ```csharp
   var tracks = albumGroup.Select(...).ToList();
   album.AverageLosslessScore = tracks.Average(t => t.LosslessScorePercent);
   album.AverageQualityScore = tracks.Average(t => t.QualityScorePercent);
   album.AverageDynamicRange = tracks.Average(t => t.DynamicRange);
   album.KeepCount = tracks.Count(t => t.Decision.StartsWith("KEEP"));
   album.InvestigateCount = tracks.Count(t => t.Decision == "INVESTIGATE");
   album.ReplaceCount = tracks.Count(t => t.Decision == "REPLACE");
   album.AlbumVerdict = album.ReplaceCount > 0 ? "REPLACE"
       : album.InvestigateCount > 0 ? "NOT SURE" : "LOSSLESS";
   ```

3. В `MainWindow.xaml` — обновить `HierarchicalDataTemplate` для `AlbumGroup`:
   ```xml
   <HierarchicalDataTemplate DataType="{x:Type models:AlbumGroup}" ItemsSource="{Binding Tracks}">
     <StackPanel Orientation="Horizontal">
       <TextBlock Text="{Binding AlbumName}" FontSize="12" Margin="8,0,4,0"/>
       <Border CornerRadius="3" Padding="4,1" Margin="4,0,0,0"
               Background="{Binding AlbumVerdict, Converter={StaticResource DecToColor}}">
         <TextBlock Text="{Binding AlbumVerdict}" FontSize="9" Foreground="#1e1e2e" FontWeight="SemiBold"/>
       </Border>
     </StackPanel>
   </HierarchicalDataTemplate>
   ```

4. В `OnTreeSelectionChanged` для AlbumGroup: показывать сводку в детальной панели
   - Добавить `[ObservableProperty] private string _albumSummary = "";`
   - При выборе альбома: `AlbumSummary = $"Средний Lossless: {album.AverageLosslessScore:F0}% | Качество: {album.AverageQualityScore:F0}% | DR{album.AverageDynamicRange:F0} | {album.KeepCount}/{album.TotalTracks} KEEP";`
   - Показать `AlbumSummary` в детальной панели над метриками

5. Build + test + commit

---

### Task 6: Подсветка выделения (баг 8)

**Files:** `MainWindow.xaml`

**Шаги:**

1. В DataGrid (MainWindow.xaml) добавить в ресурсы DataGrid:
   ```xml
   <DataGrid.Resources>
     <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#89b4fa"/>
     <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#1e1e2e"/>
     <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#45475a"/>
     <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#cdd6f4"/>
   </DataGrid.Resources>
   ```

2. В DataGridCell стиль добавить триггер:
   ```xml
   <Style.Triggers>
     <Trigger Property="IsSelected" Value="True">
       <Setter Property="Foreground" Value="{DynamicResource BgPrimaryBrush}"/>
     </Trigger>
   </Style.Triggers>
   ```

3. Build + commit

---

### Task 7: Вердикты + детализация (баг 9)

**Files:** `AudioFileViewModel.cs`, `MainWindow.xaml`, `VerdictGenerator.cs`, `Models/AnalysisResult.cs`

**Шаги:**

1. В `AudioFileViewModel.cs` — добавить computed property `VerdictLabel`:
   ```csharp
   public string VerdictLabel => Decision switch
   {
       "KEEP" => IsUpscale ? "LOSSLESS" : SampleRate >= 88200 && HiResScorePercent >= 70 ? "HI-RES" : "LOSSLESS",
       "KEEP (poor master)" => "LOSSLESS",
       "INVESTIGATE" => "NOT SURE",
       "REPLACE" => Format.StartsWith("MP3") ? $"MP3 {Mp3Bitrate}" : "REPLACE",
       _ => Decision
   };
   ```

2. В `MainWindow.xaml`:
   - Сменить DataGrid колонку «Решение»: `Binding="{Binding VerdictLabel}"`
   - Обновить DataTriggers в verdict bar (строка 431-446): заменить `KEEP`/`KEEP (poor master)`/`INVESTIGATE`/`REPLACE` на `LOSSLESS`/`NOT SURE`/`REPLACE`
   - Обновить текст: `✅ LOSSLESS` / `⚠ NOT SURE` / `❌ REPLACE` / для HI-RES `✅ HI-RES` / для MP3 `❌ MP3 320`
   - Обновить фильтр-чипсы: `LOSSLESS`, `NOT SURE`, `REPLACE`, `MP3`

3. В `Models/AnalysisResult.cs` — добавить поле `public string WhyVerdict { get; init; } = "";`

4. В `VerdictGenerator.cs` — добавить метод `GenerateWhy(AnalysisResult r)`:
   - 2-3 предложения на русском с ключевыми метриками:
   - Срез: `"Срез {CutoffFrequency:F0} Гц ({ratio*100:F0}% Найквиста) — {оценка}"`
   - Динамика: `"DR{DynamicRange:F0} — {оценка динамики}"`
   - Артефакты: `"Артефакты сжатия: {ArtifactLevel}"`
   - Вердикт: причина решения

5. В `AudioPipeline.cs` — вызвать `_verdict.GenerateWhy(result)` и записать в `result.WhyVerdict`

6. В `AudioFileViewModel.BuildMetricItems` — добавить блок «Почему?» в конец метрик:
   ```csharp
   if (!string.IsNullOrEmpty(r.WhyVerdict))
   {
       items.Add(new MetricItem { Name = "Почему?", IsHeader = true });
       items.Add(new MetricItem
       {
           Category = "Итог",
           Name = "Обоснование",
           Value = r.WhyVerdict,
           Status = "—",
           StatusColor = "#585b70",
           Description = r.WhyVerdict
       });
   }
   ```

7. Синхронизировать `ApplyFilters` в MainViewModel.cs: `ShowKeep` → фильтровать по `VerdictLabel == "LOSSLESS" || VerdictLabel == "HI-RES"`, `ShowMp3` → фильтровать по `VerdictLabel.StartsWith("MP3")`

8. Build + test + commit
